services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    depends_on:
      - ai-agent

  ai-agent:
    build: ./ai-agent
    ports:
      - "5001:5001"

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend